language: php

php:
  - 8.0
  - nightly

matrix:
  fast_finish: true
  allow_failures:
    - php: nightly
install:
  - composer i -n -o --no-suggest --prefer-dist

script:
  - composer test

cache:
  directories:
    - $HOME/.composer/cache
